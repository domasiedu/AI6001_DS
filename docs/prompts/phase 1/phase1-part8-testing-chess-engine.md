Create a test file called testMove.js inside server/chess.

Requirements:

- Import:

const getInitialBoard = require("./initialBoard");
const movePiece = require("./movePiece");

- Get initial board FEN

- Move pawn from:

e2 → e4

Coordinates:

fromRow = 6
fromCol = 4
toRow   = 4
toCol   = 4

- Print:

Original FEN
Updated FEN

Use console.log.